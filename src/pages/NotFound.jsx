import Container from '../components/Container';
import React from 'react';

const NotFound = () => {
	return (
		<Container>
			<p>Sorry, but there is no content here...</p>
		</Container>
	);
};

export default NotFound;
